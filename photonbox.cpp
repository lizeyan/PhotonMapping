#include "photonbox.h"

PhotonBox::PhotonBox (PhotonBox *lc, PhotonBox *rc, Photon *photon, int dimension, double split): _lc (lc), _rc (rc), _photon (photon), _dimension (dimension), _split (split)
{

}
